services:
  mongo:
    image: mongo:7
    container_name: jaems-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - jaems-mongo-data:/data/db

volumes:
  jaems-mongo-data: